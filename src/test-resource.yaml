defendant:
  first name: string
  middle name: string
  last name: string
  married: boolean
  ssn: string
  birth date: date
  race: selected race
  height: number
  weight: number
  address: address
  cases: many case

case:
  case number: string
  date: date
  court date: date
  guilty: boolean
  charges: many charge
  receipts: many receipt

charge:
  offense: string
  fine: number

receipt:
  paid: number
  owed: number

race:
  name: string

address:
  line 1: string
  line 2: string
  city: string
  state: string
  zip: string

user:
  username: string
  email: string
  roles: selected role
  permissions: many selected permission

role:
  name: string

permission:
  name: string